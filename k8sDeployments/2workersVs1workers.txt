kubectl scale deployment trino --replicas=2
-n default

^scaling Kubernetes to 2 coordinators

We have 2 files: kafka-1worker.JSON and kafk-2worker.JSON, from the same SQL query:

SELECT * FROM postgres.public.customers;

Listened on two separate occasions, into kafka-1worker output and kafka-2worker output. 